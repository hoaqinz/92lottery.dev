[build]
command = "npm install --legacy-peer-deps && npm run build"
output = "out"

[build.environment]
NODE_VERSION = "18.17.0"

[site]
bucket = "out"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200 