[build]
command = "npm install && npm run build"
output = ".next"

[build.environment]
NODE_VERSION = "18.17.0"

[site]
bucket = ".next"
entry-point = "src"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200 