[build]
command = "./cloudflare-build.sh"
output_directory = ".next"
base_directory = ""

[build.environment]
NODE_VERSION = "18.17.1"
NPM_VERSION = "9.6.7"

[site]
bucket = "./.next"
entry-point = "."

[env.production]
name = "production"
route = "*"

[env.staging]
name = "staging"
route = "staging.*"

# Configure Pages
[pages]
build_output_directory = ".next"
include_html_extension = false
assets_directory = ".next/static"
